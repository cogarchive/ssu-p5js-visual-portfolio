{
  "name": "@ssu/p5js-visual-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && pnpm dlx @tailwindcss/cli -i ./style.css -o ./dist/style.css && cp *.html dist/ && cp src/*.js dist/"
  },
  "devDependencies": {
    "@rspack/dev-server": "2.0.3"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "@tailwindcss/postcss": "^4.3.0",
    "p5": "^2.3.0",
    "postcss": "^8.5.15",
    "postcss-loader": "^8.2.1",
    "tailwindcss": "^4.3.0"
  }
}
